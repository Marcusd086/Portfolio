program prog11 is
        x, y, z :constant integer;
begin
        write "Enter 2 numbers: ";
        read x, y;
        write "Their sum is: ";
        z := x + y;
        writeln int2string(z);
end ;
